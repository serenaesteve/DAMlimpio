Enunciado de la actividad

Módulo: Entornos de Desarrollo
Curso: 1º Desarrollo de Aplicaciones Multiplataforma (DAM)
Evaluación: Segunda evaluación

Contexto

En esta actividad se trabajará el desarrollo de una pequeña aplicación escribiendo el código directamente desde un editor de texto. El objetivo es comprender el proceso de creación, organización y ejecución de un proyecto software sin depender de entornos de desarrollo integrados, utilizando herramientas básicas del sistema.

Tareas a realizar
-Editor de código
-Utilizar gedit como editor de texto para escribir el código.

Configurar gedit con:
-Resaltado de sintaxis del lenguaje utilizado (Python o PHP).
-Guardado correcto de los archivos con su extensión correspondiente (.py o .php).
Estructura del proyecto

Crear una carpeta para el proyecto con una estructura sencilla, por ejemplo:

proyecto/
├── src/
├── docs/
└── README.txt


Guardar el código principal dentro de la carpeta src.

Ejecución del programa
-Ejecutar el programa desde la terminal, no desde el editor.
-Python: python3 nombre_archivo.py
-PHP: php nombre_archivo.php
-Comprobar que el programa funciona correctamente.

Control de versiones (básico)
-Inicializar un repositorio Git desde la terminal.
-Realizar al menos 3 commits durante el desarrollo.
-Comentar brevemente qué cambios se han hecho en cada commit.

Documentación
Crear un archivo README.txt donde se indique:
-Lenguaje utilizado.
-Cómo ejecutar el programa.
-Breve explicación de qué hace el programa.
-Dificultades encontradas durante el desarrollo.



Respuesta:


En esta actividad del módulo Entornos de Desarrollo he realizado una pequeña aplicación web usando Python y Flask. Todo el código lo he escrito con un editor de texto, sin utilizar ningún entorno de desarrollo integrado, y he ejecutado el programa desde la terminal.

El objetivo de esta práctica es aprender cómo se crea y se organiza un proyecto de programación desde cero, entender cómo se ejecuta y ver cómo funciona una aplicación con una interfaz en el navegador.

app.py:
```
from flask import Flask, render_template, request

app = Flask(__name__)

@app.route("/", methods=["GET", "POST"])
def index():
    mensaje = ""
    nombre = ""

    if request.method == "POST":
        nombre = request.form.get("nombre", "").strip()
        if not nombre:
            mensaje = "Por favor, introduce tu nombre."
        else:
            mensaje = f"Hola {nombre}, bienvenido/a a la aplicación."

    return render_template("index.html", mensaje=mensaje, nombre=nombre)

if __name__ == "__main__":
   
    app.run(debug=True)
```


templates/index.html:
```
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Aplicación de saludo</title>
  <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
</head>
<body>
  <main class="card">
    <h1>Saludo con Flask</h1>

    <form method="POST">
      <label for="nombre">Introduce tu nombre:</label>
      <input id="nombre" name="nombre" type="text" value="{{ nombre }}" placeholder="Ej: Ana" />
      <div class="buttons">
        <button type="submit">Saludar</button>
        <a class="secondary" href="/">Limpiar</a>
      </div>
    </form>

    {% if mensaje %}
      <p class="mensaje">{{ mensaje }}</p>
    {% endif %}
  </main>
</body>
</html>
```


static/styles.css:
```
body {
  font-family: Arial, sans-serif;
  background: #f2f4f8;
  margin: 0;
  padding: 40px 16px;
}

.card {
  max-width: 520px;
  margin: 0 auto;
  background: white;
  padding: 24px;
  border-radius: 12px;
  box-shadow: 0 8px 24px rgba(0,0,0,.08);
}

h1 { margin-top: 0; }

label { display: block; margin: 12px 0 6px; }

input {
  width: 100%;
  padding: 10px;
  border: 1px solid #cbd5e1;
  border-radius: 8px;
  font-size: 16px;
  box-sizing: border-box;
}

.buttons {
  display: flex;
  gap: 10px;
  margin-top: 12px;
}

button, a.secondary {
  padding: 10px 14px;
  border-radius: 8px;
  text-decoration: none;
  border: 0;
  cursor: pointer;
  font-size: 15px;
}

button {
  background: #2563eb;
  color: white;
}

a.secondary {
  background: #e2e8f0;
  color: #0f172a;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.mensaje {
  margin-top: 16px;
  padding: 12px;
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 10px;
}
```


readme.txt:
```
Nombre del proyecto: Saludo web con Flask

Lenguaje utilizado:
Python 3 + Flask

Cómo ejecutar el programa:
1) Entrar en la carpeta del proyecto
2) (Opcional recomendado) Crear y activar entorno virtual:
   python3 -m venv venv
   source venv/bin/activate
3) Instalar Flask:
   pip install flask
4) Ejecutar:
   python3 src/app.py
5) Abrir en el navegador:
   http://127.0.0.1:5000

Breve explicación:
Aplicación web sencilla con Flask. Muestra una página con un formulario para introducir el nombre.
Al enviar el formulario, la web muestra un mensaje de bienvenida.
Incluye botón "Limpiar" que vuelve a la página inicial.

Dificultades encontradas:
- Recordar la estructura de carpetas (templates/static) requerida por Flask.
- Validación del formulario para evitar nombres vacíos.
- Realizar commits separados en momentos distintos del desarrollo.
```

millaextra2eval/
├── app.py
├── templates/
│   └── index.html
├── static/
│   └── styles.css
├── millaextra2evaluacionentornos.txt
└── readme.txt


Con esta actividad he aprendido a organizar mejor los archivos de un proyecto y a ejecutar una aplicación desde la terminal. También he aprendido lo básico para crear una aplicación web con Flask y a usar las carpetas templates y static.

Además, he utilizado Git para hacer varios commits y ver cómo funciona el control de versiones. En general, la práctica me ha servido para entender mejor cómo funciona un programa sin depender de un IDE y para ganar más confianza programando.
